В случае выполнения операции  `A == B`
в операторе  
`bool operator== (const T& other) {return this->some == other->some;}`
`A` - это `this`, а `B` - это `other`

[[Оператор noexcept]]
[[Оператор new]]